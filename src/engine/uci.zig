const misc = @import("misc");
const std = @import("std");

var buffered_inp = std.io.bufferedReader(std.io.getStdIn().reader());
var buffered_out = std.io.bufferedWriter(std.io.getStdOut().writer());
